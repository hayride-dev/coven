package hayride:morph@0.0.3;

interface types {
    variant spawn-error {
        invalid-syntax,
        forbidden,
        internal-error,
    }

    resource spawn {
        constructor();
        exec: func(name: string, args: list<string>) -> result<list<string>, spawn-error>;
    }
}