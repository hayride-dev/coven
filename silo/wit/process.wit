package hayride:silo@0.0.13;


interface process {
    /// system error numbers
    type err-no = u32;

    spawn: func(path: string, args: list<string>) -> result<s32, err-no>;
    wait: func(pid: u32) -> result<u32, err-no>;
    kill: func (pid: u32,sig: s32);
}
